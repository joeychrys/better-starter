#
# CrowdSec (production) example env file
# Copy to .envs/.production/.crowdsec and adjust values as needed.
#

# Timezone for logs and CrowdSec runtime
TZ=UTC

# Bouncer Key (Generate the key with cscli bouncers add traefik-plugin in the crowdsec container.)
CROWDSEC_BOUNCER_API_KEY=

# Optional: enroll to CrowdSec Console (if you use it)
# See: https://docs.crowdsec.net/docs/console/enroll/
#CROWDSEC_ENROLL_KEY=
#CROWDSEC_AGENT_NAME=production-next-app

# Optional: tune acquisition via env if you create your own config at runtime
# By default, our image entrypoint creates /etc/crowdsec/acquis.yaml
# to parse Traefik access logs at /var/log/traefik/access.log

# Optional: HTTP proxy settings if required in your environment
#HTTP_PROXY=
#HTTPS_PROXY=
#NO_PROXY=localhost,127.0.0.1,crowdsec

